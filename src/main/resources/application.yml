server:
  port: 8080
spring:
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    discovery:
      enabled: true
      client:
        simple:
          instances:
            iste-artifact:
              - uri: http://localhost:8080
                serviceId: iste-artifact
logging:
  level:
    root: INFO
